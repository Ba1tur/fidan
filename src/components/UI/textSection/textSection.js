import React from 'react';

import './textSection.css'

const TextSection = () => {
    return (
        <section id={'text-section'}>
            <p>
                Commissions are not set by law. All commissions are negotiable. There are no standard commission rates.
                Each seller decides total commissions paid. Commissions may vary.
            </p>
        </section>
    );
};

export default TextSection;